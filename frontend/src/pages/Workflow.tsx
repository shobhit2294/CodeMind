export default function Workflow(){return <section className="panel guide"><div className="eyebrow">UNDER THE HOOD</div><h2>From repository to evidence.</h2><p className="muted">Every stage leaves an inspectable record. A model proposes a fix; tests determine the verdict.</p>{[
 ['01','Understand','Clone a public GitHub repository into a separate workspace. Parse Python AST and JavaScript/TypeScript with Tree-sitter. Extract definitions, imports, calls and inheritance. Oversized, binary, secret and generated files are excluded.'],
 ['02','Retrieve','BM25 finds exact identifiers. Optional Sentence Transformers add semantic matches. Reciprocal rank fusion combines rankings, then graph neighbors extend the context. Relevant Git history accompanies results.'],
 ['03','Plan & investigate','Groq returns a structured plan and targeted searches. The debugger receives source citations, baseline test output and Git history. The system validates model JSON before using it.'],
 ['04','Propose a patch','The coder returns complete replacement files. CodeMind rejects path traversal, test/config edits, oversized files and invalid Python syntax. Candidate code is saved in a separate task snapshot.'],
 ['05','Execute','The original and candidate run with the same test profile in disposable Docker containers. No network, host directories, credentials or Docker socket are available to repository code. CPU, memory, process and time limits apply.'],
 ['06','Verify & remember','A reproduced baseline failure must pass after the patch without an observed regression to earn “verified”. Passing tests without a reproduction receive a weaker verdict. Failed patches feed the next attempt, up to three. SQLite retains the evidence.']
 ].map(([n,title,text])=><article className="guide-step" key={n}><span>{n}</span><div><h3>{title}</h3><p>{text}</p></div></article>)}<div className="notice">Scope: Python pytest and Node’s built-in test runner. Dependencies must be baked into the sandbox image. Static graph edges are best-effort, not full type inference. This is a local, single-user research tool.</div></section>}
